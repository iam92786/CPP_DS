#include<stdio.h>

/*
INSERTION SORT USING SINGLE LOOP

*/

int main (void)
{
    int arr[] = {0,1,1,0,0,1,1,0,1,0}
    int n = sizeof(arr)/sizeof(int);

    j = 0
    for(int i = 0; i < n; i++)
    {
        if(arr)
    }

    return 0;
}

/*
//METHOD - 1
int main (void)
{
    int arr[] = {11,33,55,22,66,88,44,33,22};
    int size = sizeof(arr)/sizeof(int);
    int temp;

    for(int i = 0; i < size; i++)
    {
        for(int j = i; j < 0; j++)
        {
            if(arr[i] < arr[j])
            {
                //swap
                temp = arr[i];
                //arr[i] = arr[j];
                //arr[j] = temp;
            }
        }

    }
    printf("DISPLAY ARRAY :\n");
    for(int i = 0; i < size; i++)
    {
        printf("%d\n",arr[i]);
    }

    return 0;
}

*/